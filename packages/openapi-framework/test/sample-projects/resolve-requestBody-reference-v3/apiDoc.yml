openapi: '3.0.0'
info:
  title: sample api doc
  version: '3'
paths: {}
components:
  requestBodies:
    Foo:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Document"
  schemas:
    Document:
      type: object
      required:
        - title
      properties:
        title:
          type: string
        path:
          type: string
