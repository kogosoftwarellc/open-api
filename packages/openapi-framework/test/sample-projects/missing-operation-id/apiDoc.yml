openapi: 3.0.0
info:
  version: "1"
  title: operationId is missing
paths:
  /foo:
    get:
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: string
